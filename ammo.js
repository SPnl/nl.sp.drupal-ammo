/*
(function ($) {
  Drupal.behaviors.AmmoToggle = {
    attach: function (context, settings) {
      $('.ammo-hidden').hide();
      $('.ammo-toggle', context).click(function(event){
        event.preventDefault();
        $(this).parent().parent().parent().find('.ammo-hidden').slideToggle(320);
      });
    }
  };
})(jQuery);
*/
